{
  "configurations": [
    {
      "name": "Launch Program",
      "program": "${workspaceFolder}/bin/www.mjs",
      "request": "launch",
      "skipFiles": [
        "<node_internals>/**"
      ],
      "type": "node",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}